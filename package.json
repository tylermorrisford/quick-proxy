{
  "name": "quick-proxy",
  "version": "1.0.0",
  "description": "a serverless proxy to the NHL API",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tylermorrisford",
  "license": "ISC",
  "devDependencies": {
    "vercel": "^32.5.5"
  },
  "dependencies": {
    "http-proxy-middleware": "^2.0.6"
  }
}
